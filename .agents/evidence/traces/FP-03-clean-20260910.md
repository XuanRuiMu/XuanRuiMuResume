# FP-03 系统干净度清理证据 2026-09-10

## 删除清单（3项，均行为不变）

1. src/ai/localEngine.ts：删本地重复`命中共享意图`，改调 intentTable 共享实现；移除冗余`共享意图表`导入。
2. src/ai/ragEngine.ts：删冗余`PatternRule`接口+`PATTERN_RULES`克隆表；`applyPatternBoost`直遍历`共享意图表`（加分0.35/0.25不变，去双重find）。
3. src/ai/models.ts：删重复模型ID字面量，`默认模型ID`复用`DEEPSEEK_MODEL`（值不变，无循环导入）。

## 未删项（仅提不删）

- `DEEPSEEK_CONTEXT_MAX`孤立导出：预存死代码，按精确修改规则只提不删。
- vite.config.js/AIChat/useAppStore：审计无重复意图映射/死分支/硬编码用户文本，未动。
- 用户可见文本均走翻译文件；数值仍可配（VITE_RAG_MIN_SCORE/GLM_MAX_TOKENS/CHAT_TIMEOUT_MS）。

## 验证

- vitest全量53文件481用例通过；相关71用例（intent4/local12/rag11/chat36/model8）通过。
- npm run lint零错（5历史警告，非本范围）；npm run build成功。
- 三轴：标准合规/需求统一映射/盲区无行为变更。
