---
import BasicLayout from "../layouts/BasicLayout.astro";
import SkillMatrix from "../components/react/SkillMatrix";
import { SITE } from "../site-config";
import "../styles/projects.css";
---

<BasicLayout
  description="技术栈工坊 - 为契合 2026 招聘市场而补充的项目集合：真实 React 项目 / 纯前端 AI 工具 / 全栈后端工程"
  title={`技术栈工坊 - ${SITE.author.fullName}`}
>
  <main class="pj-main">
    <div class="pj-shell">
      <header class="pj-head">
        <div>
          <h1>技术栈工坊</h1>
          <p class="pj-sub">
            为契合 2026 招聘市场缺口而补充的能力证明集合 · 全部基于本简历站点同一工程
          </p>
        </div>
        <a class="back-home" href="/">← 返回简历</a>
      </header>

      <p class="pj-intro">
        这里集中展示为了贴合市场需求而新增的项目：一个用 React 18 + TypeScript 写的真实可交互组件、
        一个去掉后端依赖可离线运行的纯前端 AI 求职工具、以及一个真实存在的 Python 全栈 AI Agent 后端工程。
        它们都服务于同一个目标——让简历里的每一项技术声明，都能在代码里找到对应实现。
      </p>

      <!-- a. 真实 React 项目 -->
      <section class="pj-block" aria-label="真实 React 项目">
        <div class="pj-block-head">
          <span class="pj-index">01</span>
          <div>
            <h2>真实 React 项目 · 技能矩阵</h2>
            <p class="pj-oneliner">
              用 React 18 + TypeScript + Hooks 实现的可交互组件，直接嵌入本页作为活体演示。
            </p>
          </div>
        </div>
        <div class="pj-chips">
          <span class="chip">React</span>
          <span class="chip">TypeScript</span>
          <span class="chip">Astro island</span>
          <span class="chip">Hooks</span>
        </div>
        <p class="pj-desc">
          组件使用 <code>useState</code> 管理分类筛选与选中状态，<code>useEffect</code> 驱动
          打字流动画与进度条增长，<code>useMemo</code> 做派生计算，<code>useRef</code> 操作 DOM 触发过渡。
          它是本站“真实会写 React”的实证，下方即为真实运行实例（滚动可见时自动加载）。
        </p>
        <div class="pj-demo">
          <SkillMatrix client:visible />
        </div>
      </section>

      <!-- b. 纯前端 AI 求职助手 -->
      <section class="pj-block" aria-label="纯前端 AI 求职助手">
        <div class="pj-block-head">
          <span class="pj-index">02</span>
          <div>
            <h2>纯前端 AI 求职助手 · JD 匹配分析器</h2>
            <p class="pj-oneliner">
              去掉后端依赖、可离线运行的简历 × JD 匹配工具，所有计算在浏览器内完成。
            </p>
          </div>
        </div>
        <div class="pj-chips">
          <span class="chip">TypeScript</span>
          <span class="chip">纯前端算法</span>
          <span class="chip">本地 NLP 匹配</span>
        </div>
        <p class="pj-desc">
          算法在客户端完成：技能词典（含中英文别名）关键词抽取 → 简历画像比对 → 匹配度评分与差距分析。
          无需联网、无需密钥、不调用任何后端，打开即用。可跳转到独立页面体验完整分析流程。
        </p>
        <a class="pj-action" href="/agent">打开 JD 匹配分析器 →</a>
      </section>

      <!-- c. 全栈 AI Agent 后端 -->
      <section class="pj-block" aria-label="全栈 AI Agent 后端">
        <div class="pj-block-head">
          <span class="pj-index">03</span>
          <div>
            <h2>全栈 AI Agent 后端 · FastAPI + MySQL + RAG + MCP</h2>
            <p class="pj-oneliner">
              真实存在的 Python 后端工程（位于 server/ 目录），为 AI Agent 提供完整运行时。
            </p>
          </div>
        </div>
        <div class="pj-chips">
          <span class="chip">Python</span>
          <span class="chip">FastAPI</span>
          <span class="chip">SQLAlchemy</span>
          <span class="chip">MySQL</span>
          <span class="chip">RAG</span>
          <span class="chip">MCP</span>
          <span class="chip">SSE</span>
        </div>
        <p class="pj-desc">
          架构与能力：基于 <code>ReAct</code> 运行时调度思考—行动循环；通过工具调用接入外部能力；
          以 <code>RAG</code> 检索增强私有知识，用 <code>MCP</code> 暴露标准化工具/数据源端点；
          经 <code>SSE</code> 流式推送中间过程，并由 MySQL（SQLAlchemy 异步）做持久化与轨迹回放。
          该工程为简历中“AI Agent 应用开发”提供后端实证，此处仅作展示说明，源码保持不变。
        </p>
      </section>

      <footer class="pj-foot">
        <span>// 技术栈工坊 · 同一工程内的市场向项目集合 //</span>
      </footer>
    </div>
  </main>
</BasicLayout>
