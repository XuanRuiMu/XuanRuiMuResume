# 浅色模式背景动图 — 交接文档 v2（2026-08-25）

> **下一个 AI AGENT 必读**：本文件是唯一交接来源。上一会话已完成稳定化管线并把真实 CG
> 视频循环上线（GitHub main `6625c4a`），但**循环策略有用户确认的缺陷**需要你解决。
> 先读完本文档再动手，文档里的"坑"都是实测踩出来的，不要重新踩。

## 一、当前状态（已上线）

- 浅色壁纸 = 真实 CG 视频无缝循环「燃烧的泰达希尔」（魔兽世界8.0 官方 CG 大全景镜头）
- `public/videos/teldrassil-burning-loop.mp4`：1920x810、25fps、144 帧、5.76s、H.264 crf18、4.6MB
- `public/images/teldrassil-burning-base.webp`：视频帧 0（占位/reduced-motion 降级，与视频首帧完全一致）
- 组件：`src/components/starry-background/StarryGalaxyBackground.tsx` 的 `LightWallpaper`
  = 底图 `<img>` + `<video autoplay muted loop playsinline>`（reduced-motion 时不挂视频）
- 粒子渲染器 TeldrassilFireRenderer 已删除（用户红线：必须真实画面，禁手绘/程序粒子）
- 质量实测：**静态元素（树干边缘）全帧位移 ≤1px**（用户要求 <2px）；接缝零跳变
- 验证基线：lint 0 错误 / build ✓ / vitest 384 全绿 / `npx playwright test e2e/walkthrough.spec.ts` 通过

## 二、待解决问题（你的任务）

**用户反馈（原话大意）：目前采用正放+倒放（回文循环），倒放时树上飘落的火焰粒子/余烬会
"飘回去"，物理上明显违和。**

目标：把回文循环换成**不倒放物理**的无缝循环，同时守住两条红线：

1. 静态元素绝对静止（树干等残差 <2px，验收方法见第四节）
2. 画面必须来自真实视频帧，禁止程序生成粒子/手绘/AI 生成帧
   （⚠️ 光流插帧如 RIFE/FILM 属于合成帧，用前必须先问用户）

### 推荐方向（按优先级）

1. **正放 + 自交叉淡化（xfade）**：正放序列与自身拼接，接缝处 0.5~0.8s 交叉淡化。
   ffmpeg `xfade` 滤镜一步完成。火焰是混沌运动，叠影可能读作动态模糊——必须截图逐帧检查
   余烬是否出现双影，不可接受则缩短淡化时长或换方向 2。
2. **最小跳变子段 + 短淡化**：在已稳像的帧 0-72 里找首尾火焰状态最相似的子段 [a,b]
   （对齐后帧 a 与帧 b 的静态掩膜差最小），跳变越小所需淡化越短、叠影越不可见。
3. **换素材**：CG 该镜头总长只有 3.36s（帧 0-84，其中 73+ 不可用），已无更长素材。
   备选是游戏实机「燃烧的泰达希尔」任务画面（构图不同，**必须先问用户**）。
4. 不要再做回文/倒放相关变体，用户已否决。

## 三、素材与管线（复现所需全部信息）

### 素材

- 源：B站《战争使者：希尔瓦娜斯》BV18s411w7SR，1080P 格式 ID 30080（avc1），25fps
- 大全景镜头精确范围：216.0s~219.36s（帧 0-84）；**帧 73 起匹配退化、帧 77-78 冻结帧，
  实际只用帧 0-72**（73 帧）；帧 3/28/53 是源片自带的单帧重复（40ms 停顿，可接受）
- 临时工作目录 `C:\Users\xuanr\AppData\Local\Temp\opencode\td2\` 保留了：
  `clip_raw.mp4`（215.96-219.52s 切片 89 帧，只用前 73 帧）、全部管线脚本、
  成品 `teldrassil-burning-loop.mp4`。**Temp 可能被系统清理**，没了就按上面参数重新下载切片
  （依赖已装过：yt-dlp、imageio-ffmpeg、opencv-python-headless、numpy；ffmpeg 用
  `python -c "import imageio_ffmpeg; print(imageio_ffmpeg.get_ffmpeg_exe())"` 取，系统无 ffmpeg）

### 稳像管线（已验证可复现，按序执行）

1. `python stabilize_v6.py clip_raw.mp4 73 v6_png` → 多补丁亚像素 NCC + RANSAC 仿射共识 +
   时间中位数图像参考的两轮残差精修，产出 `v6_总3.npy`（约 10 分钟）
2. `python v7_final.py` → 单轮静态掩膜约束 ECC 精修，产出 `v7f_精修总.npy`
   （⚠️ 该脚本后半段的窗口重定基实现是**错的**，它输出的 v7f_png 有 4px 漂移，只取 npy）
3. `python v8_final.py` → **正确的**窗口重定基 + 回文 PNG 序列 `v8_png/`
4. ffmpeg 编码：`ffmpeg -framerate 25 -i v8_png\f%04d.png -c:v libx264 -preset slow -crf 18 -pix_fmt yuv420p -movflags +faststart out.mp4`

**改循环策略时只需改第 3 步的 PNG 序列生成逻辑**（正放序列 + xfade 在视频层做更简单），
稳像变换（步骤 1-2 的产物）完全复用，不要重推。

### 关键参数与坐标约定

- 变换方向：`v7f_精修总.npy` 每帧是 2x3 正变换（帧0坐标→帧i坐标），配合
  `cv2.warpAffine(..., flags=INTER_LANCZOS4 | WARP_INVERSE_MAP)` 使用
- 输出画布 1920x1080，实际裁剪行 [140,950) → 成品 1920x810；
  **PNG 内坐标 = 画布坐标 - (0,140)**（上次会话曾因混淆坐标浪费大量排查时间）
- 内容安全箱：y∈[135,947]（避开顶部台标 <135 与底部遮幅 >947）
- 窗口重定基必须用**输出空间组合** `Final = M ∘ H ∘ T_w`（H=全局微缩放 g=0.984 关于
  (960,545)，T_w=平移 w≈(-73,-76)）；直接改采样矩阵的平移会经缩放线性部分（1→0.956 各帧不同）
  放大成帧间 ~4px 差异位移——这是实测踩过的坑

## 四、验收方法（不要换标准）

1. **静态结构位移**（主指标，目标 ≤2px，当前 ≤1px）：
   ```python
   高通 = img - cv2.GaussianBlur(img, (31,31), 0)   # 消除火光晕影
   # 树干左缘 PNG坐标(900,240)、树干右缘(1030,280)，64px 块，±10px 搜索
   # TM_CCOEFF_NORMED，conf>0.6 才采信
   ```
   树干是全画面唯一高对比、无歧义的静态结构，**它不动 = 相机不动**。
2. **接缝**：末帧-首帧平均绝对差 ≈ 相邻帧平均差（当前 2.48 = 2.48）。
3. **新增（针对你的任务）**：余烬方向检查——逐帧看粒子是否始终向下/向外飘，
   不得出现反向。交叉淡化的叠影帧也要检查余烬是否出现双影。

### 已证实的测量陷阱（不要用这些方法验收）

- 模板匹配在水面倒影区（PNG y≈700-760）、长矛/剑丛重复纹理区必然出假峰（conf 可达 0.99+，
  位移恰好等于搜索半径 = 平坦区 0/0 伪峰或重复纹理误配）
- ECC 多轮迭代会正反馈发散（轮1 收敛 ~2px、轮2 过冲 30px、轮3 爆炸 166px），**只做一轮**
- 全帧静态掩膜平均差对高对比边缘盲（边缘旁火光闪烁 std 高被掩膜排除，5px 漂移测不出来），
  只能做辅助指标
- 旧交接文档的"可靠锚点"（右下地面 1300,920 等）是重复纹理假匹配，全部作废

## 五、网站集成注意事项

- 改视频文件名/尺寸需同步改 3 处：组件 `StarryGalaxyBackground.tsx`、
  `StarryGalaxyBackground.test.tsx`（断言 src/loop/autoplay/muted/playsinline，
  muted 是 DOM property 不是 attribute）、`e2e/walkthrough.spec.ts`
  （断言 src + naturalWidth 1920 + currentTime 前进）
- webp 底图必须与视频首帧一致（当前从 PNG 序列 f0000.png 以 webp 质量 82 生成，105KB），
  换视频就重新生成，否则 reduced-motion 与正常模式画面不一致
- PWA 不会预缓存视频（超体积上限），按需流式加载，正常
- 墨水遮罩 `InkRevealOverlay`（z-1，浅色墨色 #7d4430）盖住壁纸（z-0），鼠标划过瞬时擦开、
  停止约 520ms 后重涂——**截图看到纯棕是遮罩静息态，不是 bug**；验证壁纸要在 mouse.move 后立即截
- 每次代码修改后：`npm run lint` + `npm run build` + `npx vitest run`（384 基线）+
  `npx playwright test e2e/walkthrough.spec.ts`，全绿后推送 GitHub main
  （仓库 https://github.com/XuanRuiMu/XuanRuiMuResume ，提交信息用中文）

## 六、历史失败方案速查（不要再试）

- vidstab/tripod 锁定/中心裁切/反向缩放/模板匹配对齐（v1-v3）：全失败
- 程序化 Canvas 粒子：用户两次否决（红线）
- 全图 ECC / 链式 ECC：被火焰污染爆炸
- 全局二次多项式拟合轨迹：真实轨迹是缓动曲线，二次拟合残差 30px+，要用局部滚动二次平滑
- 回文循环：用户否决（倒放物理违和）——你的任务就是替换它
