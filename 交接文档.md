# 交接文档：浅色模式背景更换为《神奈川冲浪里》名画动图（2026-08-25）

> 下一个 AI AGENT 必读。用户已拍板全部方向，不要重新讨论方向，直接执行以下任务。

## 〇、用户已拍板的决策

- 浅色模式背景选定：**北斋《神奈川冲浪里》名画微动循环**（候选 16），用户要求**整体调得更偏白**
- 执行流程分两步：
  1. 先给用户一个**独立的参数调整网页**（只有冲浪里这一个背景），用户自由调参后用"复制参数"功能把最终参数回传
  2. 拿到最终参数后，把简历网站浅色模式背景换成该动图并应用参数
- 红线：循环必须零跳变（现成品已构造保证，不得破坏）；画面必须来自真实公有领域画作扫描件，禁 AI 生成/程序绘制画面内容

## 一、资产转移（第一个动作，temp 目录可能被系统清理）

把以下文件从 `C:\Users\xuanr\AppData\Local\Temp\opencode\bgdemo\` 复制到持久位置（建议 `D:\xuanr\Desktop\燃烧之陨我的世界服务端\背景动图候选演示\assets\`）：

- `paintings_final\wave.png` —— 调色后的源画（1920x1312，已做过一轮提亮：gamma 0.90 / 抬白 0.05 / 饱和 0.88）
- `paintings\wave.jpg` —— 原始高清扫描件（3867x2644，大都会美术馆 CC0）
- `anim_lib.py`、`animate_paintings2.py` —— 动画渲染管线（巨浪位移函数在后者）
- 成品视频已在持久位置：`D:\xuanr\Desktop\燃烧之陨我的世界服务端\背景动图候选演示\videos\painting_wave.mp4`
  （1920x1080、250 帧 @25fps = 10s 循环、全 I 帧 H.264 crf17、末帧→首帧差比值 1.00 = 实测零跳变、32.6MB）

演示服务器（若已停）重启：`python -m http.server 5190 --directory "D:\xuanr\Desktop\燃烧之陨我的世界服务端\背景动图候选演示"` → http://localhost:5190

## 二、任务 1：参数调整网页（adjust.html）

在演示目录新建独立页面（不动 index.html）：

1. 全屏背景只放 `videos/painting_wave.mp4`（autoplay muted loop playsinline，object-fit cover）
2. 提供 P 图常用参数滑杆实时预览，建议用 CSS filter 实现（所见即所得且零重编码）：
   - 亮度 brightness / 对比度 contrast / 饱和度 saturate / 色相 hue-rotate / 色温（sepia+hue-rotate 组合模拟）/ 模糊 blur / 灰度 grayscale
   - 用户核心诉求"整体更偏白"：提供"抬白/白色浓度"类参数（可用 brightness+contrast 组合或叠加可调半透明白色层实现）
   - 默认全部中性值
3. **必做："复制参数"按钮**——一键把当前全部参数以可读格式（JSON 或完整 filter 字符串）复制到剪贴板，用户会把复制内容回传给你
4. 参数面板不要遮挡画面主体（巨浪与富士山）

## 三、任务 2：拿到用户最终参数后的网站集成

1. 参数应用方式二选一（推荐 A）：
   - A. CSS filter 直接应用到网站背景 video 元素（零重编码，所见即所得）
   - B. 烘焙进视频：把用户参数映射到调色模型（参考 temp 里 tone_adjust.py 的 伽马/抬白/饱和 三参数结构），对 wave.png 重新调色后重跑 `animate_paintings2.py` 的 `生成动画("wave", 巨浪位移)`
2. **体积优化**（必做）：32.6MB 对网站太大，目标 <10MB。重编码为 720p（背景在墨水遮罩+内容之下足够），**必须保持全 I 帧（`-g 1`）**，否则零跳变会被 P 帧泵浦效应破坏（已实测：普通 GOP 编码会在循环点产生 ~1.5 灰阶的结构性跳变）
3. 网站集成（个人简历项目，注意旧背景是燃烧泰达希尔视频，本次整体替换）：
   - 替换 `public/videos/` 背景视频；从新视频首帧重新生成 webp 底图（reduced-motion 降级用，质量 82，须与视频首帧一致）
   - 同步改 3 处代码：`src/components/starry-background/StarryGalaxyBackground.tsx`（LightWallpaper）、`StarryGalaxyBackground.test.tsx`（断言 src/loop/autoplay/muted/playsinline，muted 是 DOM property 不是 attribute）、`e2e/walkthrough.spec.ts`（断言 src + naturalWidth + currentTime 前进）
   - 浅色墨水遮罩 `InkRevealOverlay`（z-1，#7d4430）盖住壁纸（z-0）是正常设计；截图验证壁纸要在 mouse.move 后立即截，静息态纯棕不是 bug
   - 删掉旧背景文件 `teldrassil-burning-loop.mp4`、`teldrassil-burning-base.webp`，保持仓库干净
4. 验证：`npm run lint` + `npm run build` + `npx vitest run`（384 基线）+ `npx playwright test e2e/walkthrough.spec.ts`，全绿后请用户浏览器连续观看 ≥30s 确认
5. GitHub 备份：仓库 https://github.com/XuanRuiMu/XuanRuiMuResume ，main 分支，提交信息中文。注意：本地有旧背景方案（方案X）的未推送提交 b3dc706 和未提交变更，集成新背景时一并理清；2026-08-25 当天 github:443 直连被阻，推送需用户网络配合

## 四、验收标准

1. 参数网页：用户能自由调整、实时预览、一键复制参数
2. 网站集成后：循环无任何可感知跳变/倒放/凝固（动图为纯周期运动构造天然满足，但仍需真人验收）
3. 全部测试绿 + 用户本人确认效果
