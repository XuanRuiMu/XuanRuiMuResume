# 浅色模式背景动图 — 已完成（2026-08-25）

## 最终状态：任务完成

浅色模式壁纸已替换为**真实 CG 视频无缝循环**（魔兽世界8.0「燃烧的泰达希尔」大全景镜头），
粒子渲染器（TeldrassilFireRenderer）已删除。

## 交付物

- `public/videos/teldrassil-burning-loop.mp4` — 1920x810、25fps、144 帧回文循环（5.76s）、H.264 crf18、4.6MB
- `public/images/teldrassil-burning-base.webp` — 已重新生成 = 视频帧 0（占位/降级图与视频首帧完全一致）
- `src/components/starry-background/StarryGalaxyBackground.tsx` — LightWallpaper = 底图 img + `<video autoplay muted loop playsinline>`
- reduced-motion：仅静态底图，不挂载视频

## 质量指标（全部实测）

- **静态元素稳定性**：树干左/右缘（最高对比无歧义静态结构）全帧位移 **≤1px**（目标 <2px）
- **接缝**：回文循环，末帧-首帧差 = 相邻帧差（2.48 = 2.48），无跳变
- **验证链**：lint 0 错误 / build 通过 / vitest 384 全绿 / playwright 走查通过（视频真实浏览器自动循环播放确认）

## 技术要点（后续维护须知）

1. **素材**：B站《战争使者：希尔瓦娜斯》BV18s411w7SR 1080P，大全景镜头 216.0s~219.36s（帧 0-84）。
   实际采用帧 0-72（帧 73+ 匹配退化、帧 77-78 有冻结帧）。
2. **稳定管线**（临时目录 td2，可能已清理）：多补丁亚像素 NCC + RANSAC 仿射共识 →
   时间中位数图像为静态参考的残差精修 → 单轮静态掩膜约束 ECC（多轮会发散！）→
   全局微缩放 g=0.984 + 输出空间重定基 `M∘H∘T_w` → 回文 PNG → ffmpeg H.264。
3. **关键教训**：
   - 交接文档旧"可靠锚点"（右下地面等）实为重复纹理假匹配，任何单点模板锚都不可靠；
   - 重定基必须在**输出空间**组合（`M∘H∘T_w`），直接改采样矩阵平移会经缩放线性部分放大成帧间 4px 差异；
   - 静态掩膜指标对高对比边缘盲（边缘旁火光闪烁导致 std 高被排除），验收必须用高通 NCC 测确定性结构；
   - 水面倒影区/长矛重复纹理区模板匹配必然出假峰（conf 可达 0.99+），不可作验收依据。
4. **墨水遮罩交互**（InkRevealOverlay，z-1）：浅色墨色 #7d4430 盖住壁纸（z-0），
   鼠标划过瞬时擦开露出燃烧视频，停止约 520ms 后重涂——截图看到纯棕是该遮罩的正常静息态。
