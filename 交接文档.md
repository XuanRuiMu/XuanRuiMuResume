# 浅色模式背景动图 — 交接文档 v3（2026-08-25）

> **下一个 AI AGENT 必读**：本文件是唯一交接来源。回文循环倒放违和问题已解决，
> 当前线上为「纯正放 + 末段首帧定格淡入」循环。本文档记录最终方案与全部实测参数。

## 一、当前状态（已上线）

- 浅色壁纸 = 真实 CG 视频无缝循环「燃烧的泰达希尔」（魔兽世界8.0 官方 CG 大全景镜头）
- `public/videos/teldrassil-burning-loop.mp4`：1920x810、25fps、73 帧、2.92s、H.264 crf18、2.4MB
- `public/images/teldrassil-burning-base.webp`：视频帧 0（占位/reduced-motion 降级，与视频首帧一致，105KB）
- 组件：`src/components/starry-background/StarryGalaxyBackground.tsx` 的 `LightWallpaper`
  = 底图 `<img>` + `<video autoplay muted loop playsinline>`（reduced-motion 时不挂视频）
- 粒子渲染器 TeldrassilFireRenderer 已删除（用户红线：必须真实画面，禁手绘/程序粒子）
- 验证基线：lint 0 错误 / build ✓ / vitest 384 全绿 / `npx playwright test e2e/walkthrough.spec.ts` 通过

## 二、循环方案（v3，已替换回文）

**构造**：73 帧纯正放；帧 57~72 为线性淡化段——退场成分 V[i] 权重 1→0，进场成分
V[0] 定格权重 0→1；末帧 = 纯首帧定格，循环点逐像素无缝。

- 全部成分正放或定格，**无任何倒放帧**（用户否决回文的根因已消除）
- 实测验收（亚像素锚点法，树干 (900,240)/(1030,280)）：
  - 静态位移最大 1.80px（<2px ✓）
  - 循环接缝差 1.26 vs 正放段相邻均值 2.10（0.60x，完美 ✓）
  - 淡化段相邻差 1.78 ≈ 基线 2.10，无凝固感、无叠影双影（逐帧目检 ✓）
- 被否决的备选：正放回卷淡化（进场成分 V[0..15] 正放回卷）——循环点差 8.03 = 3.82x
  相邻均值，火焰场整体置换感明显，弃用
- 物理事实：火焰 6 帧（0.24s）内完全去相关，任何"回到开头"方案循环点差异下限即此；
  首帧定格淡入是唯一能实现循环点零跳变的不倒放构造

## 三、素材与管线（复现所需全部信息）

### 素材

- 源：B站《战争使者：希尔瓦娜斯》BV18s411w7SR，1080P 格式 ID 30080（avc1），25fps
- 大全景镜头精确范围：216.0s~219.36s（帧 0-84）；**帧 73 起匹配退化、帧 77-78 冻结帧，
  实际只用帧 0-72**（73 帧）；帧 3/28/53 是源片自带的单帧重复（40ms 停顿，可接受）
- 临时工作目录 `C:\Users\xuanr\AppData\Local\Temp\opencode\td2\` 保留了：
  `clip_raw.mp4`（215.96-219.52s 切片 89 帧，只用前 73 帧）、全部管线脚本、
  成品 `loop_x_freeze.mp4`（即线上版本）、帧差矩阵 `D矩阵.npy`。
  **Temp 可能被系统清理**，没了就按上面参数重新下载切片
  （依赖已装过：yt-dlp、imageio-ffmpeg、opencv-python-headless、numpy；ffmpeg 用
  `python -c "import imageio_ffmpeg; print(imageio_ffmpeg.get_ffmpeg_exe())"` 取，系统无 ffmpeg）

### 稳像管线（已验证可复现，按序执行）

1. `python stabilize_v6.py clip_raw.mp4 73 v6_png` → 多补丁亚像素 NCC + RANSAC 仿射共识 +
   时间中位数图像参考的两轮残差精修，产出 `v6_总3.npy`（约 10 分钟）
2. `python v7_npy_only.py` → 单轮静态掩膜约束 ECC 精修，产出 `v7f_精修总.npy`
   （v7_final.py 后半段的窗口重定基是**错的**，勿用；v7_npy_only.py 是只取 npy 的安全版）
3. `python v9_forward.py` → **正确的**输出空间重定基 `Final = M ∘ H ∘ T_w` + 纯正放 PNG `v9_png/`
   （H=全局微缩放 g=0.984 关于 (960,545)，T_w=平移 w≈(-73,-76)，复现值与首跑完全一致）
4. `python loop_build.py` → 生成方案X（`loop_x_png/`，已上线）与方案Y（`loop_y_png/`，已弃用）
5. ffmpeg 编码：`ffmpeg -framerate 25 -i loop_x_png\f%04d.png -c:v libx264 -preset slow -crf 18 -pix_fmt yuv420p -movflags +faststart out.mp4`

### 关键参数与坐标约定

- 变换方向：`v7f_精修总.npy` 每帧是 2x3 正变换（帧0坐标→帧i坐标），配合
  `cv2.warpAffine(..., flags=INTER_LANCZOS4 | WARP_INVERSE_MAP)` 使用
- 输出画布 1920x1080，实际裁剪行 [140,950) → 成品 1920x810；
  **PNG 内坐标 = 画布坐标 - (0,140)**
- 内容安全箱：y∈[135,947]（避开顶部台标 <135 与底部遮幅 >947）
- 窗口重定基必须用**输出空间组合** `Final = M ∘ H ∘ T_w`；直接改采样矩阵的平移会经
  缩放线性部分放大成帧间 ~4px 差异位移——实测踩过的坑
- ⚠️ imageio_ffmpeg 精简版 ffmpeg **不支持 xfade 滤镜**（Failed to configure output pad），
  淡化合成必须在 PNG/numpy 层手工做（loop_build.py 已实现）

## 四、验收方法（不要换标准）

1. **静态结构位移**（主指标，目标 <2px，当前最大 1.80px）：
   ```python
   高通 = img - cv2.GaussianBlur(img, (31,31), 0)   # 消除火光晕影
   # 树干左缘 PNG坐标(900,240)、树干右缘(1030,280)，64px 块，±10px 搜索
   # TM_CCOEFF_NORMED，conf>0.6 才采信；必须加抛物线亚像素拟合
   # （整数网格会把 ~1.5px 报成 2px 造成假超标；脚本 subpixel_qc.py / final_qc.py）
   ```
2. **接缝**：末帧-首帧平均绝对差 vs 相邻帧平均差（当前 1.26 vs 2.10 = 0.60x）。
3. **余烬方向**：方案X构造性保证（全部成分正放或定格，无倒放帧）；
   叠影双影需逐帧目检淡化段（final_qc.py 会导出检查帧）

### 已证实的测量陷阱（不要用这些方法验收）

- 模板匹配在水面倒影区（PNG y≈700-760）、长矛/剑丛重复纹理区必然出假峰（conf 可达 0.99+）
- 整数像素模板匹配在 2px 边缘摆动 ±0.5px，必须亚像素拟合才能下结论
- ECC 多轮迭代会正反馈发散，**只做一轮**
- 全帧静态掩膜平均差对高对比边缘盲，只能做辅助指标
- 旧交接文档的"可靠锚点"（右下地面 1300,920 等）是重复纹理假匹配，全部作废

## 五、网站集成注意事项

- 改视频文件名/尺寸需同步改 3 处：组件 `StarryGalaxyBackground.tsx`、
  `StarryGalaxyBackground.test.tsx`（断言 src/loop/autoplay/muted/playsinline，
  muted 是 DOM property 不是 attribute）、`e2e/walkthrough.spec.ts`
  （断言 src + naturalWidth 1920 + currentTime 前进）
- webp 底图必须与视频首帧一致（当前从 v9_png/f0000.png 以 webp 质量 82 生成，105KB），
  换视频就重新生成，否则 reduced-motion 与正常模式画面不一致
- PWA 不会预缓存视频（超体积上限），按需流式加载，正常
- 墨水遮罩 `InkRevealOverlay`（z-1，浅色墨色 #7d4430）盖住壁纸（z-0），鼠标划过瞬时擦开、
  停止约 520ms 后重涂——**截图看到纯棕是遮罩静息态，不是 bug**；验证壁纸要在 mouse.move 后立即截
- 每次代码修改后：`npm run lint` + `npm run build` + `npx vitest run`（384 基线）+
  `npx playwright test e2e/walkthrough.spec.ts`，全绿后推送 GitHub main
  （仓库 https://github.com/XuanRuiMu/XuanRuiMuResume ，提交信息用中文）

## 六、历史失败方案速查（不要再试）

- vidstab/tripod 锁定/中心裁切/反向缩放/模板匹配对齐（v1-v3）：全失败
- 程序化 Canvas 粒子：用户两次否决（红线）
- 全图 ECC / 链式 ECC：被火焰污染爆炸
- 全局二次多项式拟合轨迹：真实轨迹是缓动曲线，二次拟合残差 30px+，要用局部滚动二次平滑
- 回文循环：用户否决（倒放物理违和）——已替换
- 正放回卷淡化（方案Y）：循环点差 3.82x 相邻均值，火焰场置换感明显——已否决
- ffmpeg xfade 滤镜：imageio_ffmpeg 精简版不支持——PNG 层手工合成
